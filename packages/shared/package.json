{
  "name": "@apployd/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src --ext .ts",
    "test": "npx vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
